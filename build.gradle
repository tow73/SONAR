plugins {
  id "org.sonarqube" version "4.2.1.3168"
}

sonarqube {
  properties {
    property "sonar.projectKey", "tow73_Sonar"
    property "sonar.organization", "tow73-sonar-qube-test"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}
